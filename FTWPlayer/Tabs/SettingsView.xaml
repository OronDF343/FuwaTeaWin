<UserControl x:Class="FTWPlayer.Tabs.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <TabControl Style="{DynamicResource SettingsTabControlStyle}">
        <TabItem Style="{DynamicResource SettingsTabItemStyle}">
            <TabItem.Header>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <ContentPresenter Content="{DynamicResource Example1Content}" />
                    <TextBlock Grid.Row="1" Text="Example 1" FontWeight="Bold" />
                </Grid>
            </TabItem.Header>
        </TabItem>
        <TabItem Style="{DynamicResource SettingsTabItemStyle}">
            <TabItem.Header>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <ContentPresenter Content="{DynamicResource Example2Content}" />
                    <TextBlock Grid.Row="1" Text="Example 2" FontWeight="Bold" />
                </Grid>
            </TabItem.Header>
        </TabItem>
    </TabControl>
</UserControl>